{
  "name": "s2d-crm",
  "version": "1.7.1",
  "description": "Shelves to Drawers RVA - The BOS: funnel, texting, self-serve booking, job status tracking, basic finances. Pure Node.js core modules, with a single exception: nodemailer for Gmail SMTP.",
  "main": "src/server.js",
  "type": "commonjs",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "nodemailer": "^10.0.10"
  }
}
